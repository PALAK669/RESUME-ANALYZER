# MongoDB connection string
# Local example: mongodb://127.0.0.1:27017/resume-analyzer
# Atlas example: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/resume-analyzer
MONGO_URI=

# Secret used to sign JWT auth tokens — any long random string
JWT_SECRET=
